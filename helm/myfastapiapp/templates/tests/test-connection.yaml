apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myfastapiapp.fullname" . }}-test-connection"
  labels:
    {{- include "myfastapiapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myfastapiapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
